const vect = []
const llenarvector = (vec, tam) => {
    for (let i = 0; i < tam; i++) {
        vect[i] = Math.round(Math.random() * 100);

    }
    return vect;
}
const ordenarVector = (vec) => {
    let aux;


}

const vector = []
llenarvector(5)