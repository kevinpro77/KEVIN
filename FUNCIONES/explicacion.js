/*function primo(num) {
    var cont = 0;
    for (let i = 0; i < num; i++) {
        num % 2 == 0 ? cont++ : cont = cont;

    }
    //cont>1?return`no es primo`: return `no primo`;
    return cont > 1 ? `no es primo` : ` primo`;
    //if (cont > 2) {
    //   return 'no primo'
    // } else {
    //    return 'primo'
    //}
}
console.log(primo(40));


var x = 13
x % 2 == 0 ? console.log('par') : console.log('impar')
    // condicion? valor si es verdadero: valor si es falso
if (x % 2 == 0) {
    console.log('es par')
} else {
    console.log('impar')
}
*/

/*ejercicio*/
//escriba una funcion que lea una cadena de texto y diga cuantas vocales y cuantas consonantes y cuantos que no son ni vocales ni consonantes

/*function contar_vocales(vocales) {
    let conso="aeiou";
    let count = 0;
    
    for (let  letra  of  vocales ) {
        if (conso.includes(letra.toLowerCase())) {
            count++;
        }
    }
 return count
}
const cadena = ('bienvenidos a ADSI')
const result = contar_vocales(cadena);
console.log(result);

*/
/*
function contar_consonantes(vocales) {
    let conso = "bcdfghjklmnñpqrstvwxyz";
    let count = 0;

    for (let letra of vocales) {
        if (conso.includes(letra.toLowerCase())) {
            count++;
        }
    }
    return count
}
const cadena = ('me gusta programar')
const result = contar_consonantes(cadena);
console.log(result);

*/